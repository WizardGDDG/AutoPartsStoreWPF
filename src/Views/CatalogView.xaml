<UserControl x:Class="AutoPartsStoreWPF.CatalogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="КАТАЛОГ ТОВАРОВ" FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,10"/>

        <WrapPanel Grid.Row="1" Margin="0,0,0,10">
            <TextBox x:Name="txtSearch" Width="150" Margin="5" Padding="5" ToolTip="Поиск"/>
            <Button Content="Найти" Click="Search" Margin="5" Padding="10,5"/>

            <ComboBox x:Name="cmbCategory" Width="150" Margin="5" Padding="5" ToolTip="Категория">
                <ComboBoxItem Content="Все" IsSelected="True"/>
                <ComboBoxItem Content="Детали двигателя"/>
                <ComboBoxItem Content="Ходовая часть"/>
                <ComboBoxItem Content="Электрика"/>
                <ComboBoxItem Content="Расходные материалы"/>
            </ComboBox>

            <TextBox x:Name="txtMinPrice" Width="80" Margin="5" Padding="5" ToolTip="Цена от"/>
            <TextBox x:Name="txtMaxPrice" Width="80" Margin="5" Padding="5" ToolTip="Цена до"/>
            <Button Content="Фильтр" Click="Filter" Margin="5" Padding="10,5"/>
        </WrapPanel>

        <DataGrid x:Name="dgProducts" Grid.Row="2" AutoGenerateColumns="False" 
                  CanUserAddRows="False" IsReadOnly="True" Height="300">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Название" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Категория" Binding="{Binding Category}" Width="150"/>
                <DataGridTextColumn Header="Цена" Binding="{Binding Price, StringFormat=C}" Width="100"/>
                <DataGridTextColumn Header="Наличие" Binding="{Binding Stock}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
            <Button Content="Добавить в корзину" Click="AddToCart" 
                    Background="#2ECC71" Foreground="White" FontWeight="Bold" 
                    Padding="20,10" Margin="5"/>
            <Button Content="Обновить" Click="Refresh" 
                    Background="#3498DB" Foreground="White" FontWeight="Bold" 
                    Padding="20,10" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>